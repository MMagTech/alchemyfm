[pytest]
testpaths =
    audiomuse-plugins/alchemy_fm_bridge/tests
    backend/tests
pythonpath =
    backend
    audiomuse-plugins/alchemy_fm_bridge/tests
asyncio_mode = auto
markers =
    postgres: requires TEST_DATABASE_URL (plugin database tests)
