# Per-station Liquidsoap scripts are generated by the backend:
#   /data/liquidsoap/stations/<slug>.liq
#   /data/liquidsoap/manifest.json
#
# The liquidsoap container runs a supervisor that starts one process per
# enabled station. New stations go live within a few seconds — no manual restart.
